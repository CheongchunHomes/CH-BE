server:
  port: 18080

spring:
  application:
    name: homes
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

jwt:
  secret: "fjiefj3oja3r89321-r891-vm2ur892gur9g302125930525-532"
  access-token-expiration-ms: 3600000